# TODO

- [x] Fix A/V toggle: ensure backend returns both audio/video URLs (or enough info for toggling) even when NewPipeClient provides only one stream.
- [x] Fix frontend toggling: remove/relax disabling condition that blocks the toggle when `video_stream_url` / `audio_stream_url` is missing.
- [ ] Verify: play YouTube item (audio), toggle to video; play YouTube item (video), toggle to audio.
- [ ] Run any available checks/tests/build.


